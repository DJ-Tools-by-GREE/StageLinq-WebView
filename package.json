{
  "name": "dj-decks-visualizer",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run -w backend dev",
    "build": "npm run -w frontend build && npm run -w backend build",
    "start": "npm run -w backend start",
    "show": "node scripts/show.mjs",
    "fresh": "npm install && npm run build && node scripts/show.mjs",
    "lite": "npm run -w backend build && LITE_MODE=1 npm start"
  },
  "dependencies": {
    "@gree44/stagelinq": "^3.0.9",
    "sacn": "4.6.2"
  }
}