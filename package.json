{
  "name": "dj-decks-visualizer",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run -w backend dev",
    "build": "npm run -w frontend build && npm run -w backend build",
    "start": "npm run -w backend start",
    "deploy": "npm run build && pm2 restart stagelinq-webview || pm2 start npm --name stagelinq-webview -- start && pm2 logs stagelinq-webview"
  },
  "dependencies": {
    "@gree44/stagelinq": "^3.0.9",
    "sacn": "4.6.2"
  }
}
